<article class="card-project">
  <figure class="card-figure">
    <img src={Astro.props.image} alt={Astro.props.title} />
    <figcaption>
      <p>{Astro.props.area}</p>
    </figcaption>
  </figure>
  <a class="card-link" href={Astro.props.link}>Veja o case completo</a>
</article>

<style>
  .card-project {
    border-radius: 4px;
    position: relative;
    box-shadow: 2px 2px 4px 0px rgba(0, 0, 0, 0.1);
    overflow: hidden;
  }

  .card-figure {
    width: 100%;
    max-height: 354px;

    figcaption {
      position: absolute;
      inset-block-start: 18px;
      inset-inline-start: 24px;
      filter: invert(0);
      mix-blend-mode: difference;
    }

    img {
      display: block;
      width: 100%;
      height: auto;
      object-fit: cover;
    }
  }

  .card-link {
    display: block;
    padding-block: 24px;
    padding-inline: 32px;
    font-weight: 500;
    font-size: 1rem;
    color: var(--color-white);
    text-align: center;
    text-decoration: none;
    background: var(--color-dark-secondary-100);
    box-shadow: 0px 1.84px 1.84px 0px #ffffff40 inset;
  }
</style>
